import { useState } from 'react';
import { PREFIX_STORE, THEME_MODE } from '../constants/global';

export const useTheme = () => {
  // const currentTheme: string =
  //   localStorage.getItem(`${PREFIX_STORE}theme`) || THEME_MODE.LIGHT;
  // const [isTheme, setIsTheme] = useState<string>(currentTheme);
  // const onChangeTheme = (theme: THEME_MODE.DARK | THEME_MODE.LIGHT) => {
  //   setIsTheme(theme);
  //   localStorage.setItem('theme', `${PREFIX_STORE}${theme}`);
  // };
  // return { isTheme, onChangeTheme };
};
